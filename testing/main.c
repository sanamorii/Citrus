#include <stdio.h>
#include <stdlib.h>
#include <math.h>

enum rank {a, b, c};

int main(int argc, char const *argv[])
{
    int array[3] = {2, 4, 1};
    printf("array: %d\n", array[b]);
    return 0;
}
